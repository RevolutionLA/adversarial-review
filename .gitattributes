# 统一按 LF 处理，避免 shell 脚本在 Linux/macOS 上出现 bad interpreter
* text=auto eol=lf

# Windows 专用脚本保留 CRLF
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.zip binary
