---
name: adversarial-review
description: 开发完成后的三方对抗式代码评审闭环：蓝军（敌意审查，须给证据链）→ 第三方（独立审计，不信双方文档、只读代码，专找"修改引入的新缺陷"）→ 中立裁定（逐条采纳/驳回并纠正判读）。当用户说"蓝军评审""第三方复核""对抗性审查""红蓝互搏""三方评审""交叉验证""发布前审查""帮我挑刺""adversarial review""red team review""pre-release review""critique this""find bugs in my code"或要求提升代码质量、准备发布前把关时使用。也适用于用户想把当前会话变成"多个 Agent 互搏"来提升代码水平的场景。
license: MIT
compatibility: 需要一个能派发独立子代理（subagent）的宿主（Claude Code / DeepSeek Harness / Cursor / Codex 等）。若无子代理能力，降级为单 agent 串行扮演三角色，并在报告中如实声明独立性已显著削弱。
metadata:
  author: RevolutionLA
  version: "1.1"
---

# 三方对抗式代码评审（Blue Team / Third Party / Adjudication）

## 这个 skill 要解决什么

单人或单 AI 写代码，最大的风险不是"不会写"，而是：

1. **用未验证的乐观假设说服自己推迟修复**（"这个兜底应该够了吧"）；
2. **测试提供假安全感**（测试全绿，但功能其实早坏了）；
3. **为了修 A 而引入 B**（新写的修复代码本身没被独立审视）。

这三个问题**靠"再仔细看一遍"解决不了**，只能靠**结构化的对抗关系**：

| 角色 | 立场 | 硬性约束 |
|---|---|---|
| **蓝军** | 敌意审查——假设代码会出事，去**证明** | 每条结论必须附证据链（`文件:行号` / 实测命令 / 宿主源码）；禁止"建议加强鲁棒性"这类空话 |
| **第三方** | 独立审计——**不信任任何一方文档**，直接读代码 | 必须逐条复核"声称修了的是否真修了"，并**专门寻找修改过程引入的新缺陷** |
| **中立裁定** | 对第三方意见**逐条采纳 / 驳回 / 改级** | 必须由**与蓝军不同的 agent** 担任；防止第三方定级偏高或推理错误，也防止蓝军为自己的结论辩护 |

**关键不是"多找两个 AI 看看"，而是顺序与互不信任**：第三方不信开发团队，裁定方不信蓝军和第三方。任一环节缺失，闭环就破。

> ⚠️ **能力边界（必须向用户如实说明）**
> 子代理与主代理通常是**同一模型**，这不是真正独立的第三方。其价值来自**角色约束 + 强制证据**，而非"另一个 AI 的观点"。
> - ✅ 能有效抓出：代码级错误、逻辑漏洞、测试造假、自相矛盾、遗漏分支、声明与实现不符；
> - ❌ 抓不出：**双方共有的知识盲区**（例如对某个上游行为的一致误解）。若某个结论依赖外部系统行为，必须要求**实测验证**，而不是两个 agent 互相点头。
>
> 向用户汇报时，不要说"经独立第三方验证无误"，要说"经同模型不同角色的对抗审查"。

> 🔁 **降级模式（宿主无子代理能力时）**
> 若宿主不提供 `subagent`，只能由**同一个 agent 串行扮演三角色**。此时：
> - 独立性**已被显著削弱**，第 2、3 步的价值大幅下降；
> - 必须在报告开头显式声明"本次为降级模式，角色由同一 agent 串行扮演"；
> - 不得使用"独立复核""第三方验证"等措辞。
> - 有条件时优先换一个有子代理能力的宿主，而不是将就。

---

## 执行流程

### 第 0 步：确定档位与范围

先问用户或从上下文判断档位（默认**标准档**）：

| 档位 | 配置 | 适用 |
|---|---|---|
| **轻档** | 1 个蓝军（聚焦关键项：正确性 / 兼容性 / 测试有效性）| 小改动、时间紧 |
| **标准档**（默认）| 蓝军全量 → 第三方复核 → 中立裁定，共 3 轮 | 一般功能版本 |
| **重档** | 标准档 + 多路子代理并行交叉验证（兼容性 / 功能安全 / 生态共存 分开审）| 发布前、重大重构、涉及上游兼容 |

**同时确认**：
- 审查基线 = 哪个 commit / 工作区？（用 `git rev-parse --short HEAD` 与 `git status` 记录，写进报告表头）
- 报告落盘位置（默认 `docs/review/`，见"报告归档"一节）

**动手前必做**：把待审状态**提交或至少暂存**，让蓝军拿到可复现的基线。工作区一片混乱时审查结论不可靠。

---

### 第 1 步：蓝军评审（敌意审查）

用 `subagent` 起一个子代理，**禁止**用 `subagent_fork`（fork 会继承你的思路，失去独立性）。

**提示词模板**：

```
你是「蓝军」——专门对本项目做**对抗性（敌意）代码评审**的审查者。你的任务不是夸奖，是**找出问题并证明它们真实存在**。

## 审查对象
- 仓库：<绝对路径>
- 基线：<commit sha>（工作区/分支状态：<描述>）
- 本次改动范围：<改动摘要或 git diff 范围>
- 项目背景：<这是什么项目、用户是谁、最关键的质量红线是什么>
- 声称已完成的内容：<开发团队的自述清单>

## 立场要求（重要）
1. **不采信开发团队的说明文档与注释**。注释说"已修复"，你要去代码里确认；文档说"性能没问题"，你要自己算。
2. **假设它会出事，然后去证明**。对每个关键路径问：「什么输入/环境/时序会让它坏？」
3. **优先攻击这些目标**：
   - 兼容性：上游/宿主/依赖换代后会不会崩？崩溃半径多大（只坏自己 vs 拖垮整体）？
   - 错误处理：有没有"只处理了一半"的分支？失败路径是否被静默吞掉？
   - 测试有效性：测试是否**只做字符串自匹配**、只断言实现细节、或覆盖不到最关键场景？
   - 声明与实现不符：文档/CHANGELOG/注释声称的，代码里真的做了吗？
   - 修改引入的新问题：为修 A 而写的代码，是否引入了 B？
   - 资源与生命周期：定时器、监听器、缓存、状态残留是否清理？多实例/多窗口是否互相干扰？
   - 安全：注入、越权、明文、未校验输入。
4. **必须实证**：能跑就跑（`node --check`、测试、最小复现脚本）；不能跑就给出严谨的推理链。
5. **禁止空话**：不要写"建议增加健壮性""考虑边界情况"这类无指向结论。每条必须能落到具体代码位置与具体后果。

## 输出格式（严格遵守）

先给一张**缺陷总表**，按严重度排序：

| 编号 | 级别 | 一句话 | 维度 |
|---|---|---|---|
| B1 | 🔴/🟠/🟡 | ... | 兼容性/功能/安全/测试/治理 |

级别定义：
- 🔴 = 会导致崩溃、数据损坏、或向用户/生态广播错误信息
- 🟠 = 功能在真实使用模式下失效、或声明与实现不符
- 🟡 = 需要修但不阻塞发布

然后**逐条展开**，每条包含：
- **证据链**：`文件:行号`（必须真实，禁止编造行号）+ 复现命令或推理过程
- **触发条件**：什么情况下会出事
- **用户可见后果**：用户会看到什么现象（用大白话）
- **修复方向**：具体怎么改（给代码或明确步骤）

最后给：
- **优先级路线图**：P0（发布前必修）/ P1 / P2
- **做得对、值得保持的**（防止改动时误伤好设计）
- **未验证项**：如实声明哪些结论你没能验证（这比假装验证过更有价值）

## 环境
<工作目录、可用的测试命令、上游源码/依赖的位置>
```

---

### 第 2 步：第三方复核（独立审计）

拿到蓝军报告后，用**新的** `subagent`（不要复用蓝军那个，避免它为自己的结论辩护）。

**提示词模板**：

```
你是「第三方独立复核方」。开发团队已按蓝军评审做了整改，你要**独立验证整改是否真实落地**，并**找出整改过程新引入的缺陷**。

## 你的立场（关键）
1. **不信任双方的任何文档**——不采信蓝军报告的行号，也不采信开发团队的回应说明。**直接读代码**，行号自己重新定位（整改后行号会位移）。
2. **两个方向都要查**：
   - **A. 声称修了的，是否真修了？** 逐条核对蓝军的每项意见，在代码里找到对应实现，判断修法是否正确、是否只修了表面。
   - **B. 修的过程有没有修出新问题？** 这是你**最重要的独有职责**。整改代码通常很新、没被审视过。重点看：新代码的边界条件、状态残留、生命周期、与新老逻辑的交互。
3. **特别关注"测试假安全感"**：新加的测试是否真的能捕获它声称覆盖的场景？把测试**故意改坏**（在脑内或实际改）验证它会不会失败——不会失败的测试等于没写。
4. 对"刻意推迟"的项目：判断推迟理由是否成立。**如果理由是"已被某某兜底"，必须去验证那个兜底是否真的覆盖了该场景**（这一步极容易出错，是本次历史上真实翻车点）。

## 输入
- 仓库：<绝对路径>
- 当前基线：<commit / 工作区状态>
- 蓝军意见：<逐条列出，或指向报告文件路径>
- 开发团队回应：<回应文档路径或摘要>

## 输出格式

### 第一部分：蓝军意见逐项验证表
| 编号 | 蓝军意见 | 验证结果 | 证据（文件:行号） |
|---|---|---|---|
| B1 | ... | ✅ 属实落地 / ⚠️ 部分 / ❌ 未落地 / 🔁 修法有误 | ... |

### 第二部分：新发现的缺陷（你的核心产出）
编号 T1/T2/T3…，每条给出：问题、位置（文件:行号）、**触发链**（一步步推到后果）、定级、修复方向。
必须区分"真实执行路径"与"理论可能"——如果当前 UI 走不到，要说明可达性，别虚高定级。

### 第三部分：发布门结论
- 代码是否达到可发布状态？
- 还有什么**硬性阻塞项**（例如未提交、CI 未跑、版本号不一致）？
- 你的保留意见（哪些你没能验证）

## 要求
- 所有行号必须是你**自己读到的真实行号**，禁止照抄蓝军报告。
- 每条结论给出可复查的证据，不接受"看起来没问题"。
```

---

### 第 3 步：中立裁定（采纳意见）

**必须起一个全新的 `subagent` 作为"中立裁定方"**，不是蓝军，也不是第三方。

> ⚠️ **为什么不能复用蓝军做裁定**：裁定的对象是"第三方对蓝军意见的复核"。若让蓝军自己裁定，它同时是**当事人**和**法官**——它会系统性驳回针对自己的 T 项，并为自己原有的定级辩护。这与本 skill 第 1、2 步「禁止复用 agent，避免它为结论辩护」的核心原则**直接冲突**。
> "蓝军不信第三方"这个设定通过**提示词立场**表达（要求裁定方独立复验、不采信任何一方行号），**不是**通过复用 agent 身份实现。
> 降级选项：宿主子代理数量受限时，可用 `send_message` 复用蓝军 agent 做裁定，但**必须在报告里声明"裁定方与蓝军为同一 agent，独立性已降级"**。

**提示词模板**：

```
你是「中立裁定方」。蓝军提出了意见，第三方对蓝军意见做了复核。现在由你**逐条裁定第三方意见是否成立**。

## 你的立场（关键）
1. **你既不是蓝军也不是第三方**。你对**两方结论都不采信**，只采信你自己读到的代码。
2. **行号全部自己重新定位**（整改后行号已位移），禁止照抄任何一方的行号。
3. 对第三方每一条：**采纳 / 部分采纳 / 驳回**，并说明理由。
4. **允许下调或上调定级**——如果第三方把可达性很低的防御性代码定成高危，明确指出"定级与实际可达性不匹配，会稀释对真问题的注意力"。
5. **同时纠正蓝军的判读错误**——包括蓝军原报告里定级虚高、证据不实、或推理链条断裂的条目。你不是蓝军的辩护人。
6. **重点检查第三方的修复建议是否会踩坑**（例如它建议的改法是否会与现有语义冲突）。
7. 如果发现开发团队用"已被某某兜底"作为推迟理由，去验证那个兜底是否真的覆盖该场景；**这类推理极容易错**。

## 输出
- 对第三方每条 T 项的裁定（采纳/驳回/改级）+ 理由
- 对蓝军每条 B 项在复核后的**最终状态**
- **实质分歧**（如有），以及谁对——若两方都没说对，给出你的结论
- **合并执行清单**：把两份报告合成一张 P0/P1/P2 表，标注来源与改动量估计
- 一句话总结三方关系：蓝军定义"修什么"，第三方验证"修没修对、有没有修出新问题"，本报告裁定"双方的结论是否成立"

## 输入
<仓库路径、第三方报告路径、蓝军原报告路径、当前基线>
```

---

### 第 4 步：主代理执行整改

拿到合并清单后，**由你（主代理）亲自改代码**，不要外包——整改需要全局一致性判断。

整改纪律：
1. **每条改动配一个回归用例**。特别是：无测试覆盖的修复必复发。
2. **测试要断言"行为"，不要断言"实现字符串"**。反例：断言 CSS 里"包含某类名"——这类名会随构建漂移，测试永远绿而功能早坏。正例：模拟真实输入，断言**用户可观察的结果**。
3. **改完必须自己复验**：`node --check`、全量测试、以及关键修复的**最小复现脚本**（证明修前坏、修后好）。
4. **诚实记录**：如果发现自己在上一轮文档里写了错误结论，**显式标注作废**，不要悄悄改掉。三方评审的价值就在于留下可追溯的纠正记录。
5. 不要为了让报告好看而虚报。做不到就写"推迟"并给出**经过验证的**理由。

---

### 第 5 步：收口与归档

1. **汇报时明确区分**：哪些已修、哪些推迟（附理由）、哪些未能验证。
2. 报告的归档按用户偏好（见下）。
3. 若涉及发布：确认版本号一致、测试全绿、文档与实际改动一致（**CHANGELOG 里声称改了的文件，必须真的改了**——这是历史上真实翻车点）。

---

## 报告归档

**默认策略按项目可见性决定**（选错会造成死链或泄密，两边都麻烦）：

| 项目类型 | 默认做法 |
|---|---|
| **私有仓库 / 未发布** | 写入 `docs/review/` 并**加入 `.gitignore`**，内部攻防记录不外泄 |
| **公开仓库** | 要么正常提交（作为质量流程展示），要么 gitignore **并且彻底清理引用** |

gitignore 写法：

```gitignore
# internal review notes (kept locally, deliberately NOT published)
/docs/review/
```

命名约定：
```
docs/review/
├─ BLUE-TEAM-REVIEW-<version>.md       # 蓝军（含宿主/上游源码级证据）
├─ RESPONSE-<version>.md               # 开发团队逐条回应（采纳/部分/推迟/驳回）
├─ THIRD-PARTY-REVIEW-<version>.md     # 第三方独立复核（T 项）
└─ ADJUDICATION-<version>.md           # 中立裁定（对双方结论的最终裁定）
```

⚠️ **如果报告被 gitignore，务必同时清理 README/CHANGELOG 里指向它们的链接**——否则发布后是死链。用 `grep -rn "docs/review" README.md CHANGELOG.md docs/` 检查。

---

## 提示词设计要点（改模板时别丢这些）

| 要点 | 为什么关键 |
|---|---|
| **角色 + 立场** 而非"帮我审一下" | 立场决定它能发现什么。"挑刺"和"检查"产出完全不同 |
| **必须给证据链**（文件:行号 / 实测） | 禁止空话，也让你能复查。编造行号要能被抓到 |
| **禁止采信文档** | 文档是最容易骗人的东西。文档失实是真实翻车点 |
| **专找"修改引入的新缺陷"** | 独立角色，是标准流程里最容易漏的一环 |
| **裁定方必须与蓝军不同 agent** | 当事人不能当法官，否则裁定退化为自我辩护 |
| **逼它声明"未验证项"** | 把不确定性显式化，避免用自信语气掩盖无知 |
| **要求区分可达性** | 防止把理论可能定成高危，稀释注意力 |
| **允许裁定方驳回** | 第三方也会错；没有裁定环节就变成"意见越多越好" |

---

## 常见的错误用法

- ❌ **用 `subagent_fork` 起蓝军** → 继承你的思路，只会附和。
- ❌ **让蓝军自己裁定第三方意见** → 当事人当法官，针对蓝军的批评会被系统性驳回。
- ❌ **让同一个 agent 既审又改又复核** → 它会为自己的结论辩护。
- ❌ **把三方报告当成"通过认证"** → 同模型互搏抓不出共同盲区；依赖外部行为的关键结论必须实测。
- ❌ **第三方只验"修了没"，不找新问题** → 丢掉一半价值。
- ❌ **改了代码不补测试** → 下轮评审同样的问题会再次出现。
- ❌ **报告写得很漂亮但代码没动** → 立刻修订，别把流程做成表演。
