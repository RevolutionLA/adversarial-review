# THIRD-PARTY-REVIEW — v2.0.0

> **复核方**：第三方独立复核方（不采信双方任何文档，行号全部自行重新定位）
> **真实基线**：`3cb6cea`（⚠️ **不是**任务书给的 `bfb58ee`，见 T1）
> **完整改动范围**：`4637e50..3cb6cea`（19 文件，+1284/−344）
> **只读确认**：审查结束 `git status --porcelain -uall` 为空，48 个测试夹具全部位于 `$env:TEMP\tpr-audit`

---

## 第一部分：整改声称逐项验证表

| 编号 | 声称的修复 | 验证结果 | 证据 |
|---|---|---|---|
| **B1** | 块标量解析重写 + 回归测试 | ⚠️ **解析器属实修好；测试断言强度不足** | `scripts/validate-skill.mjs:44-119` |
| **B2** | SKILL.md 新增「配套文件」段 | ✅ 属实落地 | `SKILL.md:31-37`、`:71`、`:78`、`:88`、`:100` |
| **B3** | 迁移到 `skills/adversarial-review/` | ✅ 属实落地，路径全部可达 | 5 处 SKILL.md + 4 处 README + quickstart 实测 OK |
| **B4** | 失效行号改为显式声明 | ✅ 属实落地（方式可接受） | `examples/sample-review.md:6-13` |
| **B5** | CI 链接检查改用 `check-links.mjs` | ✅ 属实落地，能真正失败 | `scripts/check-links.mjs:64-85` |
| **B6** | `skill-spec.md` 补充渐进式披露约束 | ❌ **未落地（虚假声明）** | 见 T2：blob 哈希未变 |
| **B7** | 安装脚本加备份 + 版本提示 | ✅ 代码落地；⚠️ **端到端从未执行** | `install.sh:43-51`、`install.ps1:56-63` |
| **B8** | 模板外置 + 「占位符约定」表 | ✅ 属实落地 | `references/prompt-templates.md:6-17` |
| **B9** | 19 维度表 | ✅ 落地**且真接进流程** | `review-dimensions.md:18-38`；接入 `SKILL.md:71,80,90,102` |
| **B10** | `verify-install.sh` 参数处理提前 | ⚠️ **功能正确，但有死代码** | 见 T4 |
| **B11** | 备份而非删除 | ✅ 属实落地 | `install.sh:46-49`、`install.ps1:58-61` |

### B1 精细裁定：解析器真修了，但测试有盲区

第三方用 **28 个构造用例对新旧两版做对照实验**：

| 用例 | 旧版（4637e50） | 新版（HEAD） |
|---|---|---|
| `>` 折叠块 | exit=0 ✅通过，**descLen=1** | descLen=97 ✔ |
| `\|` 字面块 | exit=0 ✅通过，**descLen=1** | descLen=91 ✔ |
| `>-` / `\|-` | ✅通过，**descLen=2** | 正确 ✔ |
| `>+` / `\|+` / `>2` | descLen=2 | 正确识别 ✔ |
| 块后跟其他字段 | descLen=1 | 正确终结 ✔ |
| 块内空行 / 缩进不一致 | descLen=1 | 正确 ✔ |
| 空块标量 | ✅**误通过** | 正确报错 ✔ |
| `compatibility` 块标量 | 全部塌成 1 字符 | 正确解析，>500 真报错 ✔ |
| 含 `#` / 冒号的合法值 | descLen=1 | 不误伤 ✔ |
| CRLF + 折叠块 / 嵌套 metadata | descLen=1 | 通过 ✔ |

**结论**：旧版确实把块标量读成 1–2 字符且仍报「✅ 通过」，蓝军 B1 完全属实；新版对 28 个用例行为全部正确，**未发现反向退化**。

---

## 第二部分：新发现的缺陷

### T1 · 🔴 任务书基线已过期

- **事实**：任务书写明基线 `bfb58ee`（"当前 HEAD，工作区干净"）。第三方实测开工时 `git status` **非空**（5 改 + 2 未跟踪）；审查期间 `3cb6cea` 落地收编了这些文件。
- **触发链**：以 `bfb58ee` 为基线评审 → 遗漏 `3cb6cea` 的 7 文件改动（+411/−17）→ 其中含 `verify-install.sh` 修复与 `docs/` 归档 → 发布门判断基于残缺快照。
- **定级**：🔴（对评审任务本身是真实可达的流程缺陷）
- **修复方向**：以 `3cb6cea` 为发布门基线；**提交后冻结工作区再启动评审**。

### T2 · 🔴 B6 整改声明失实：`skill-spec.md` 内容零改动

- **证据**：`4637e50:references/skill-spec.md` 与 `HEAD:skills/adversarial-review/references/skill-spec.md` 的 **blob 哈希完全相同**（`7a21fd8dcc5d3bd6fb412c2421957cfa2944d9ed`），`git diff --find-renames` 显示 `0 insertions, 0 deletions` —— 纯 `git mv`。
- **文档声称**（`RESPONSE-2.0.0.md:82-86`）：补充了「正文 < 500 行」「渐进式披露」「引用只能一层深」。
- **实际**：全文**没有**这些内容。L15 只有 v1.1 即存在的 `references/` 存在理由说明；L26 的「500」是 `compatibility` **字段**上限，与正文行数无关。
- **触发链**：第三方/读者据 RESPONSE 表勾选「✅ 已修」→ 不再复核 → 该规范约束至今缺失 → 后续作者写出超长 SKILL.md 无人依据。
- **定级**：🔴 —— 在一个以「不采信文档、必须有证据链」为核心卖点的 skill 仓库里，整改回应文档出现**可被哈希证伪的虚假声明**，直接击穿其自身方法论。
- **修复方向**：要么真正补写这三条约束，要么把 B6 改为「❌ 未完成」。**诚实记录优先于表格好看。**

### T3 · 🟠 CI step 名实不符：声称检查 PowerShell，实际只检查 bash

- **位置**：`.github/workflows/validate-skill.yml:46-50`
- **代码**：step 名为「校验 shell / PowerShell 脚本语法」，step 体只有两行 `bash -n`。**全文无任何 pwsh/powershell/ps1 检查**。
- **触发链**：`install.ps1`（80 行，Windows 主安装路径）语法损坏 → CI 全绿 → 合并 → Windows 用户 `irm | iex` 直接崩。**这与蓝军 B5 是同一类缺陷，只是换了个位置**——开发团队修好了链接检查，却新造了一个名实不符的装饰性 step。
- **定级**：🟠（`install.ps1` 当前语法正确，非当前故障；但检查永远不可能失败）

### T4 · 🟡 `verify-install.sh` 死代码：`FOUND` 被立即重置

- **位置**：`verify-install.sh:12-17` —— 查找循环的结果被第 17 行 `FOUND=""` 立刻清空，随后重跑一遍同样循环。
- **定级**：🟡 —— **实测三种场景全部正确**，B10 功能目标已达成。这是可读性问题，不是行为缺陷，不虚高定级。

### T5 · 🟡 回归测试的 `descLen` 断言无法区分折叠/字面语义

- **根因**：`check()` 只断言输出含 `description 长度: N /`。而**同内容下折叠块与字面块字符数完全相等**（实测两行用空格连接 = 111，用换行连接 = **同样 111**）。
- **定级**：🟡（测试**能**捕获块标量被完全忽略，故非永真测试；但存在真实盲区）

---

## 第三部分：蓝军覆盖度稽核

**蓝军有实质结论且核实属实的**：B1（新旧对照实验完全复现）、B2（`Select-String` 结果为 0）、B5（`fail` 从不置 1）、B3。

**蓝军判读不严谨的**：
- **B7 证据时点问题**：蓝军称本机装 v1.0（255 行）。第三方实测**现在**已是 181 行 v2.0。蓝军观测在整改前成立，但 RESPONSE 标「✅ 已修」却未提供安装动作证据——**`~/.claude/skills/` 下的 `.bak.*` 并非脚本执行产物**（是主代理手动 `Move-Item` 的），说明备份逻辑**从未端到端执行过**。

**蓝军完全没看的区域（第三方补充覆盖）**：
1. **CI 其余 step 是否也能失败** —— 蓝军只审了链接检查那一个 step，漏掉了 T3。
2. **整改文档自身的真实性** —— 蓝军审的是代码，没有反过来核验 RESPONSE 每一条声明。**B6 因此漏网。这是本轮评审最大的方法论缺口：蓝军没有对「整改回应」本身做敌意审查。**
3. **新增测试的断言强度** —— 未对新增测试文件本身做突变验证。
4. **`check-links.mjs` 自身的正则健壮性**。
5. **`install.ps1` / Windows 路径** —— 双方都未实际执行。

**关于 B12（行内注释）**：第三方实测确认未修复，认为取舍**合理**（正则切分确实会破坏含 `#` 的合法值，如 `C#`）。**但不认同只在 RESPONSE 记录而不在文档提示用户。**

**关于 B13（嵌套 metadata）**：第三方**未能构造出会被误判的 skill**，维持推迟可接受。

---

## 第四部分：测试有效性裁定（15 个突变测试）

### ✅ 真有效的用例（会失败）

| 突变 | 结果 |
|---|---|
| M1 块标量指示符不再识别（退回旧行为） | ✅ 捕获（5 通过 4 失败） |
| M3 折叠块只取第一行 | ✅ 捕获 |
| M4 字面块只取第一行 | ✅ 捕获 |
| M7 name/目录名一致性校验被移除 | ✅ 捕获 |
| M8 NAME_RE 放宽 | ✅ 捕获 |
| N1 description 必填校验被删除 | ✅ 捕获 |

**结论**：RESPONSE 声称的"修前必然失败"**成立**——旧版下这 4 个用例的 `descLen` 会读到 1，与断言全部不符。

### ❌ 逃逸（测试有盲区）

| 突变 | 结果 | 为何逃逸 |
|---|---|---|
| **M2/N5 折叠块与字面块语义互换** | ❌ **9 通过 0 失败** | 字符数相同（111==111） |
| **M5/N6 块后不重置 blockStyle** | ❌ **9 通过 0 失败** | 未覆盖"块后紧跟另一个块" |
| **M6/N4 删除「正文为空」校验** | ❌ **9 通过 0 失败** | 无用例覆盖空正文 |
| **N2 删除 description >1024 上限校验** | ❌ **9 通过 0 失败** | 无用例构造超长 description |
| **N3 删除 compatibility >500 上限校验** | ❌ **9 通过 0 失败** | 无用例构造超长 compatibility |

**关键测试假安全感**：测试注释宣称覆盖了"块标量被误解析为 1 字符"——这部分属实。但它**未覆盖任何长度上限校验**，而这正是蓝军 B1 的核心危害（"1024 字符上限形同虚设"）。**修复了描述里的危害，却没有为危害本身写用例。**

---

## 第五部分：发布门结论

### 是否达到可发布状态？

**基本达到，但需先修正一处文档失实与两处 CI 名实不符。**

代码层面质量良好：三个脚本在真实 HEAD 上全部 exit=0；28 个解析器边界用例全对；10 个 markdown 的本地链接全部可达；**B3 迁移没有留下断链**。

### 硬性阻塞项

| # | 阻塞项 | 依据 |
|---|---|---|
| **1** | **B6 整改声明失实** | blob 哈希未变（T2）；必须改为"未完成"或真正补写 |
| **2** | **基线未冻结** | 任务书给 `bfb58ee`，实际 HEAD 已是 `3cb6cea`（T1） |
| 3 | CI step 名实不符（T3） | 违反本项目自定红线「检查必须真的能失败」 |

### 未阻塞但建议随版修复

- T4 死代码；T5 测试断言盲区（**补 4 个用例即可覆盖 5 个逃逸突变中的 4 个**）。
- 本机 `.bak.*` 非脚本产物 → **建议在隔离 HOME 下实测一次再宣称「已修」**。

### 保留意见（未能验证）

1. 未在 Linux/macOS 实测安装脚本（URL 用 HEAD 请求验证均 200，但未真实下载解包）。
2. 未验证 skills.sh 是否真索引本仓库。
3. 未在多 agent 宿主实测 `subagent` 行为差异。
4. 突变测试中临时改过 `validate-skill.mjs`，每次均已还原，最终 `git status` 干净。
5. 审查期间仓库被提交（`3cb6cea`），早期探测与落地后重跑结果一致，但 `verify-install.sh` 的观测跨越两版，已分别标注。

### 一句话总结

**B1（解析器）、B3（迁移）、B5（链接检查）、B9（维度闭环）是真实且有质量的修复；B6 是经哈希证伪的虚假声明；新增的 9 个测试锁住了原始缺陷但漏掉全部长度校验，属「半有效」；整改过程确实新引入了缺陷——一个名实不符的 CI step（T3）和一处死代码（T4）。**

**最值得警惕的一点**：这个以「不采信文档、禁止空话」为卖点的仓库，其自身的整改回应文档出现了失实声明，而蓝军因为只审代码、不审整改回应，没能发现它。
